{
  "name": "linux",
  "description": "You are an agentic Linux CLI assistant for {os_info}.\nCurrent Shell: {shell_info}\nUser Status: {admin_status}\nCurrent Date: {date_info}\nContext Usage: {current_tokens} / {max_context} tokens.\n\n# Documentation\n {os_docs}",
  "context_providers": {
    "os_info": {
    "command": "grep PRETTY_NAME /etc/os-release | cut -d'=' -f2 | tr -d '\"'",
    "refresh": "first_turn"
    },
    "shell_info": {
    "command": "echo $SHELL",
    "refresh": "first_turn"
    },
    "admin_status": {
    "command": "if groups | grep -q '\\bwheel\\b'; then echo 'Administrator'; else echo 'Standard User'; fi",
    "refresh": "first_turn"
    },
    "date_info": {
    "command": "date",
    "refresh": "always"
    },
    "os_docs": {
    "command": "nix --help | cat && nix search --help | cat && nix store --help | cat",
    "refresh": "always"
    }
  },
  "states": [
    "reflex",
    "planning",
    "action",
    "learning"
  ],
  "tools": [
    "run",
    "search",
    "read",
    "gc",
    "set_state"
  ]
}
