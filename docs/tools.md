---
when_to_read: "Adding or modifying a tool and its schema."
related: ["context-providers.md", "evaluators.md"]
---
# Tools

`@ask_tool` decorator and handler signature. Tools are AUTO-DISCOVERED via pkgutil in ask.py — no manual import step.

> Content migrates from `developer-guide.md` §"Adding Custom Tools" (with the
> incorrect "import the tool in ask.py" step corrected during migration).
