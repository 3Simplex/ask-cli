---
when_to_read: "Looking up a config.json key or its default."
related: ["providers.md", "evaluators.md"]
---
# Configuration

config.json location, keys, and defaults.

> Content migrates from `developer-guide.md` §"Configuration" (config options table).
> NOTE: a known default drift (max_turns) exists and is intentionally NOT fixed here.
